set(prefix "@CMAKE_BINARY_DIR@/native-projection-prefix")
set(work "/tmp/mkchad-v1/container-tools-c11/native-projection-integration")
file(REMOVE_RECURSE "${prefix}" "${work}")
execute_process(COMMAND "@CMAKE_COMMAND@" --install "@CMAKE_BINARY_DIR@" --prefix "${prefix}" RESULT_VARIABLE install_result)
if(NOT install_result EQUAL 0)
  message(FATAL_ERROR "native projection install failed")
endif()
execute_process(COMMAND bash "@CMAKE_CURRENT_SOURCE_DIR@/tests/native_projection_test.sh" "${work}" "${prefix}/bin/container-tools" RESULT_VARIABLE test_result)
if(NOT test_result EQUAL 0)
  message(FATAL_ERROR "native projection fixture integration failed")
endif()
