# ct-mount-plan-v1

`ct-mount-plan-v1` is the immutable product-major-1 manifest grammar. U2 only
records its identity in the package; producing or consuming manifest bytes is
deferred until the runtime and selected-root units.
